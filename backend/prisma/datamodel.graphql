type User {
  id: ID! @unique
  name: String!
  email: String! @unique
  password: String!
  isActive: Boolean! @default(value: false)
  Role: Role @default(value: "USER")
  createdAt: DateTime!
  updatedAt: DateTime!
}

enum Role {
  ADMIN
  USER
}
