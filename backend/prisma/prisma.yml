endpoint: ${env:PRISMA_ENDPOINT}
# secret: ${env:PRISMA_SECRET}
datamodel: datamodel.graphql
# - database/enums.graphql
# - database/company.graphql
seed:
  import: seed.graphql
hooks:
  post-deploy:
    - graphql get-schema -p prisma    - graphql get-schema -p prisma